# ==========================================
# Champions League Simulator - Requirements
# ==========================================

# ------------------------------
# API + HTTP Networking
# ------------------------------
requests>=2.31.0
# Required to make HTTP requests or call external services

openai>=1.0.0
# DeepSeek API uses the OpenAI-compatible SDK
# The code uses "OpenAI()" class even though the base URL is DeepSeek's

# ------------------------------
# Testing Framework
# ------------------------------
pytest>=7.4.0
# Needed for running unit tests in the tests/ directory
